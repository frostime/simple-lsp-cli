{
  "name": "simple-lsp-cli",
  "version": "1.0.0",
  "description": "Zero-dependency CLI for invoking LSP methods — designed as an Agent tool",
  "license": "MIT",
  "type": "module",
  "bin": {
    "simple-lsp-cli": "./dist/cli.js",
    "slsp": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cross-spawn": "^7.0.6",
    "vscode-jsonrpc": "^8.2.1"
  }
}
